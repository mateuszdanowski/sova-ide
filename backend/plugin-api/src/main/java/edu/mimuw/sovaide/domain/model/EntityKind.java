package edu.mimuw.sovaide.domain.model;

/**
 * Specifies the supported kinds of code entities, such as class, interface, record, enum, or annotation.
 */
public enum EntityKind {
	CLASS,
	INTERFACE,
	RECORD,
	ENUM,
	ANNOTATION,
	OTHER
}
